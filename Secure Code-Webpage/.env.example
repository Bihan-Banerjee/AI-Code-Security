# Frontend (Vite) environment — copy to .env.development / .env.production
# Base URL of the backend API (NO trailing /api — the app adds /api itself).
VITE_API_URL=http://localhost:5000
VITE_APP_ENV=development
